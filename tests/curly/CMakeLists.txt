# We might support earlier versions, too, but try to use a more recent one.
cmake_minimum_required (VERSION 2.8)

# Recurse into subdirectory for test of Curly PUT request with predefined data
# for request body.
add_subdirectory (put-body)
